# Variables de entorno del servidor (solo disponibles en el servidor)
# Estas variables NO se exponen al cliente

# Clave secreta de Stripe para procesar pagos
# Obtén tu clave en: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...

# Variables de entorno del cliente (disponibles en servidor y cliente)
# Estas variables se exponen al cliente (prefijo NEXT_PUBLIC_)

# URL base de la API (usualmente la misma que SITE_URL en desarrollo)
# Ejemplo: http://localhost:3000 o https://tu-dominio.com
NEXT_PUBLIC_API_URL=http://localhost:3000

# URL base del sitio web
# Ejemplo: http://localhost:3000 o https://tu-dominio.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000
